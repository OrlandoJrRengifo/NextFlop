"use client";

import React from "react";
import MediaLayout from "@/media/components/Layout";

export default function ReactflixLayout({ children }) {
  return <MediaLayout>{children}</MediaLayout>;
}
